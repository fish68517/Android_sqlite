主题明确完整主题
1:  服务于该主题的各知识点的合理使用
2: 界面设计 必须用到RecycleView和底部导航栏（导航方式自定）
  必须用到至少5种其他不同类型的UI控件  Activity和Fragment使用  必须使用Activity和Fragment，且两者总数不少于6个（不包括登陆和注册页面）（每个页面必须满屏布局且功能设计合理）利用Bundle进行Activity与Activity、Activity与Fragment、Fragment与Fragment之间的数据传输
3: 数据持久化存储:  使用 SharedPreferences（在登陆页面使用 SharedPreferences 来保存是否勾选了记住密码以及密码值）
    使用数据库（SQLite本地数据库）