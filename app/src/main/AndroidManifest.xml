<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/> <!-- Android 13+ 需要 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <!-- 1. 声明相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 2. (可选但推荐) 声明应用需要相机硬件。
         android:required="false" 表示即使设备没有相机，应用也能安装，
         这对于您的应用是合适的情况，因为相机只是一个可选功能。 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />


    <!-- 必须：前台服务权限 (Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" /> <!-- Android 14+ 建议 -->


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="课程管理系统"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:name="com.myapplication.app.MyApplication"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">


        <receiver android:name="com.myapplication.app.AlarmReceiver"/>

        <activity android:name="com.myapplication.app.activity.MainActivity" android:exported="true">

        </activity>
        <activity android:name="com.myapplication.app.activity.AddCourseActivity" />
        <activity android:name="com.myapplication.app.activity.AddHomeworkActivity" />
        <activity android:name="com.myapplication.app.activity.AddExamActivity" />

        <activity android:name="com.myapplication.app.activity.AddMediaActivity"/>

        <service android:name="com.myapplication.app.MusicService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true"/>

        <activity android:name="com.myapplication.app.activity.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.myapplication.app.activity.RegisterActivity"
            android:exported="true">
        </activity>







        <!-- 声明 FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>



    </application>

</manifest>